ssh-keygen -t rsa -C "email"
git config --global user.name ""
git config --global user.email ""

git分支的基本使用：

//新建分支并将其合并到master
git branch branch_name //新建本地分支
git branch branch_name base_branch_name
git push origin branch_name //合并到远程分支

git checkout master //切到master分支
git pull origin master //拉去最新master
git merge branch_name //将分支合并的master
git push origin master //提交到master

git branch -r //查看所有远程分支
git branch -a //查看所有分支
git branch -d branch_name //删除本地分支
git push origin :branch_name //删除远程分支


git撤销commit基本操作



git回退commit基本操作
